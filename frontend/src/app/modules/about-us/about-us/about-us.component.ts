import { Component } from '@angular/core';

@Component({
  selector: 'app-about-us',
  templateUrl: './about-us.component.html',
  styleUrl: './about-us.component.scss'
})
export class AboutUsComponent {

}

/*
    *standalone (=false)
    If a component isn't standalone, it means that Angular doesn't allow to this components to exist alone by its own since it has to rely on something (a module)

*/
